Conventions
=============

- Models, training runs, and evaluation are defined by ``.toml`` files which specify model model & training params as well as datasets and input ``.laz`` files
- 
- Code conventions are a mix of VSCode defaults, those in the ``.vscode/settings.json`` and in ``.ruff.toml``